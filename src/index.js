import ReactDOM from "react-dom/client";
// css
import App from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <div className="wrap">
    <App />
  </div>,
);
